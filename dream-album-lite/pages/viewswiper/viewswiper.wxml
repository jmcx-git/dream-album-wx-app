<view wx:if="{{refresh}}">
    <view catchtap="init" style="padding-bottom:{{winHeight*750/winWidth-748}}rpx" class="loading_page">
        <image class="loading_pic" src="../../image/makeloading.png" />
        <text class="loading_text">{{refreshtip}}</text>
    </view>
</view>
<view wx:else>
    <view hidden="{{pptHidden}}">
        <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" style="height:{{winHeight}}px;">
            <block wx:for="{{loopPreImgs}}" wx:key="imgs">
                <swiper-item>
                    <image wx:if="{{extraPic==undefined}}" data-img="{{item}}" src="{{item}}" class="slide-image" bindtap="showAlbum" style="width:{{winWidth}}px;height:{{winHeight}}px;"/>
                    <image wx:elif="{{item==currentLink}}" src="{{item}}" class="slide-image" bindtap="showIndex" style="width:{{winWidth}}px;height:{{winHeight}}px;"/>
                    <image wx:else src="{{item}}" class="slide-image" data-img="{{item}}" bindtap="showAlbum" style="width:{{winWidth}}px;height:{{winHeight}}px;"/>
                </swiper-item>
            </block>
        </swiper>
    </view>
</view>
<!--<view wx:else>
    <image wx:if="{{extraPic==undefined}}" src="{{imgUrl}}" animation="{{animationData}}" data-img="{{imgUrl}}"   bindtap="showAlbum" bindload="loadPic" style="height:{{picHeight}}rpx;width:{{picWidth}}rpx;"/>
    <image wx:elif="{{imgUrl==currentLink}}" src="{{imgUrl}}" animation="{{animationData}}" bindtap="showIndex" bindload="loadPic" style="height:{{picHeight}}rpx;width:{{picWidth}}rpx;"/>
    <image wx:else src="{{imgUrl}}" animation="{{animationData}}" data-img="{{imgUrl}}" bindtap="showAlbum" bindload="loadPic" style="height:{{picHeight}}rpx;width:{{picWidth}}rpx;"/>
    <view hidden="{{reloadHidden}}">
        <button type="primary" bindtap="reloadPlay" class="replayButton">重播</button>
        <button wx:if="{{extraPic!= undefined}}" type="primary" bindtap="showIndex" class="reDirectButton">我也来创建一个相册</button>
    </view>
</view>-->



